# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

An AI-powered interviewer application that uses the Anthropic Claude API (key available in `.env` as `ANTHROPIC_API_KEY`).

## Current State

This project is in early initialization. No build system, framework, or application code has been set up yet. When a stack is chosen, update this file with build/lint/test commands and architecture details.

## Environment

- `.env` contains `ANTHROPIC_API_KEY` — load it before making API calls (never commit it; it's gitignored)
